## 1. 优化 Light 模式阴影与卡片样式

- [x] 1.1 在 `web/src/index.css` 的 `.theme-light` 部分覆盖 `.glass-card` 的 `box-shadow` 为浅色柔和阴影，增加内发光
- [x] 1.2 覆盖 `.theme-light` 模式下的 `.site-tile` (网站卡片) 默认文字颜色、边框以及各网站 Hover 样式
- [x] 1.3 覆盖 `.theme-light` 模式下的 `.mini-stat-item .stat-number` 状态文字颜色与文字发光，确保数值清晰可读

## 2. 优化磁盘容量条与进度显示

- [x] 2.1 覆盖 `.theme-light` 下的 `.gauge-bar-track` 轨道背景色以及 `.gauge-bar-fill` 填充渐变与阴影
- [x] 2.2 覆盖 `.theme-light` 下的 `.gauge-free` 字色为深绿色

## 3. 构建并运行验证

- [x] 3.1 运行前端打包构建，并在浏览器中确认调整后的亮色主题视觉完美
